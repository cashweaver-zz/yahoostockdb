db(stockpredictor)

function list:
    __init__(self)
    update(self, db_path)
    load_db(self)
    internet_on(self)
    get_latest_remote_date(symbol)
    get_latest_local_date(symbol)
    table_exists_for(symbol)
    create_tables_for(symbol)
    update_tables_for(symbol, sdate, edate)

flow:
    stockpredictor.db.update()
        if internet_on()
            lrd = get_latest_remote_date(symbol)
            if not lrd == ""
                if table_exists_for(symbol)
                    lld = get_latest_local_date()
                    update_tables_for(symbol, lld, today)
                else
                    create_tables_for(symbol)
                    update_tables_for(symbol, the_beginning, today)
            elif the db has a table for this symbol
                drop tables
            else
                skip
